let i = 1;
let fila = ""; // Creamos una cadena vacía para ir acumulando los números

while (i <= 10) {
  fila += i + " "; // Concatenamos el número y un espacio
  i = i + 1;
}

console.log(fila); // Imprimimos toda la fila en una sola línea

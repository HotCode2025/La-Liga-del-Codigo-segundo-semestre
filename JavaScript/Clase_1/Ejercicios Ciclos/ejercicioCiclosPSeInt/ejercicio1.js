let i = 1; // Definir i como entero e inicializar en 1

while (i <= 10) { // Mientras i sea menor o igual a 10
  console.log(i); // Escribir i en consola (una línea por número)
  i = i + 1; // Incrementar i en 1 para evitar bucle infinito
}